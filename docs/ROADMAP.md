# خارطة الطريق — zol

هذا الملف هو المرجع الدائم لخطوات التطوير القادمة حتى لا تضيع. يُحدَّث مع كل مرحلة.
(انظر أيضًا [`COMPETITIVE_ANALYSIS.md`](./COMPETITIVE_ANALYSIS.md) للتحليل التنافسي الكامل.)

## تنسيق بين جلستين متوازيتين (2026-08-10)

يعمل على هذا الفرع (`claude/global-apps-improvements-mbx7yy`) جلستان بالتوازي.
تقسيم الملكية المتفق عليه (وارد في مذكرة تنسيق أرسلتها الجلسة الأخرى):

| المسار | المالك |
|---|---|
| `android/**`, `ios/**`, `web/**`, `.github/workflows/**` | هذه الجلسة (بناء Android/CI) |
| `lib/**`, `test/**` | الجلسة الأخرى (طبقة الذكاء الاصطناعي وواجهات Flutter) |
| `pubspec.yaml` | مشترك ⚠️ — يُدمَج فوق المحتوى الحالي، لا يُستبدَل الملف كاملًا |

هذه الجلسة التزمت الحدود أعلاه بالكامل: لم تُعدَّل سوى `android/build.gradle.kts`
و`.github/workflows/build-apk.yml`. الحالة الحالية موثّقة أدناه تحت "بناء APK
أندرويد عبر GitHub Actions".

## ما أُنجز حتى الآن ✅

- رحلة العميل كاملة: تفاصيل المنتج → توليد بمراحل → 3 نسخ بدرجة توافق → نشر أو طباعة.
- كتالوج مطبوعات بمقاسات وكميات وتسعير فوري (منتج + توصيل + ضريبة 15%).
- تتبع الطلبات بخط زمني، مكتبة «إعلاناتي»، وضع داكن/فاتح، تنقل سفلي.
- مولّد نصوص قوالبي (يعمل بلا إنترنت) — يُستبدل لاحقًا حسب استراتيجية الذكاء أدناه.
- خط Tajawal مضمّن + CanvasKit محلي للويب (يعمل خلف الشبكات المقيدة).
- **رفع صورة منتج حقيقي** من معرض الجهاز (`image_picker`) — الخطوة 1. ✅
- **محرك قوالب التصميم** (`lib/widgets/ad_design_preview.dart`): تركيب صورة
  المنتج على قالب بألوان النبرة مع النص والهاشتاقات، وتصديره **صورة PNG**
  حقيقية عبر المشاركة — أول جزء من الطبقة 1 (بقي عزل الخلفية). ✅
- **حفظ دائم** للإعلانات والطلبات والتفضيلات (`shared_preferences`) —
  الخطوة 3 بنسختها المحلية. ✅
- **تحديد موقع التوصيل على الخريطة** (OpenStreetMap + GPS) — منقول من
  zadgo2 فرع split-customer مع دروسه (حماية الموقع الافتراضي والمُحاكى). ✅
- **خريطة متابعة الطلب** بمسار المطبعة → العميل وسائق متحرك (محاكاة تُستبدل
  ببث حقيقي من تطبيق السائق لاحقًا). ✅
- **عزل خلفية صورة المنتج على الجهاز** (`lib/services/background_remover.dart`):
  خوارزمية Dart نقية (تقدير لون الخلفية من الحواف + تعبئة انتشارية) تعمل على
  كل المنصات، مع خيار التبديل أصلية/معزولة، وامتناع آمن عن العزل عند الخلفيات
  المعقدة — تكتمل الطبقة 1 بأساسها، ويُضاف نموذج تجزئة ML لاحقًا للخلفيات
  الصعبة عبر نفس الواجهة. ✅
- **حساب التاجر** (الخطوة 4 بنسختها المحلية): تسجيل/دخول/خروج بكلمة مرور
  مجزأة (SHA-256 + ملح) وجلسة محفوظة تُسترجع بعد إعادة التشغيل، بطاقة الحساب
  في الإعدادات وتحية بالاسم في الرئيسية — الواجهة `register/login/logout`
  تُستبدل داخليًا بـ Firebase Auth (نمط zadgo2) عند بناء الخادم. ✅
- **بوابة الدفع** (الخطوة 5): تكامل «ميسر» منقول من zadgo2 بدروسه — ودجت
  البوابة الجاهز (خارج نطاق PCI-DSS)، المفتاح المنشور عبر `--dart-define`
  لا في المستودع، ولا يُعدّ طلب مدفوعًا إلا بتأكيد البوابة. اختيار طريقة
  الدفع في الطلب (بطاقة / عند الاستلام)، ومع غياب المفتاح تعمل محاكاة
  موسومة «تجريبي». الاشتراك الاحترافي (109 ر.س شهريًا) عبر نفس المسار
  ويزيل العلامة المائية من التصاميم. ✅
- **التوجيه التلقائي لأقرب مطبعة** (الجزء المحلي من الخطوة 6): شبكة مطابع
  شريكة (`lib/models/print_shop.dart`) وكل طلب بموقع خريطة يُسند تلقائيًا
  للمطبعة الأقرب (هافرساين)، وتظهر على خريطة المتابعة وفي تفاصيل الطلب.
  المتبقي: لوحة تحكم المطابع (تحتاج خادمًا). ✅
- **Brand Kit** (الخطوة 8 — نمط Canva): لون العلامة وشعار المتجر من
  الإعدادات، يُطبَّقان تلقائيًا على كل التصاميم المولَّدة ويُحفظان دائمًا. ✅
- **الهوية البصرية**: اسم التطبيق أصبح **zol** على كل المنصات (أندرويد،
  iOS، ويب)، وشعار مصمّم في `assets/brand/` (SVG) مرسوم أيضًا أصلًا
  بـ CustomPainter (`lib/widgets/zol_logo.dart`) فيبقى حادًا في أي حجم،
  وأيقونات التطبيق الـ25 مولّدة من الشعار نفسه. ✅
- **شاشة الترحيب** لأول تشغيل (بنية منقولة من مستودع wasl): ثلاث صفحات
  تشرح رحلة zol — توليد، طباعة، توصيل. ✅
- **محرك القوالب الكامل** (إكمال الطبقة 1): **عشرة قوالب** مختلفة التركيب
  (جريء · منقسم · ملصق · بقعة ضوء · أنيق · عرض خاص · رأي عميل · إطار ·
  عاجل · دائري)
  (جريء، منقسم، ملصق، بقعة ضوء، أنيق، عرض خاص) مع شريط اختيار يعرض معاينة
  حيّة لكل قالب على منتج التاجر نفسه، و**استخراج تلقائي للون المسيطر من
  صورة المنتج** (`palette_extractor.dart`) فيبدو التصميم مصمَّمًا لهذا
  المنتج لا قالبًا عامًّا. أولوية اللون: Brand Kit ← لون المنتج ← النبرة،
  مع ضمان تباين المنتج عن خلفيته تلقائيًا. ✅
- **معاينة المطبوع قبل الشراء** (`print_mockup.dart`): التصميم معروضًا على
  بنر بعيون تثبيت، ورقة استيكرات مقصوصة، كرتَي أعمال متداخلتين، وستاند
  رول أب بقاعدته — يتغيّر فورًا مع نوع المطبوع ومقاسه. نمط
  VistaPrint/Printify، ومرسوم على الجهاز من التصميم نفسه بلا صور جاهزة. ✅
- **تفعيل مكتبة «إعلاناتي»**: الإعلان المحفوظ يُفتح بالنقر لإعادة تصديره
  أو تغيير قالبه أو طلب طباعته، وصورة المنتج تُضغط (`image_store.dart`)
  وتُحفظ معه فيبقى قابلًا للاستخدام بعد إغلاق التطبيق. ✅
- **تبويب «القوالب» — معرض متصفَّح** (`templates_screen.dart`): سبع فئات
  حسب الغرض (منشور إنستغرام، ستوري، ريلز، بنر، كرت أعمال، ملصق، رول أب)
  مع بحث عربي بالمرادفات، ومعاينة حيّة لكل قالب. النقر ينقل إلى إنشاء
  الإعلان بالصيغة والمنصة والقالب مُعبّأة مسبقًا — فينقلب القمع: يتصفّح
  التاجر ويُلهَم أولًا ثم يرفع صورته. ✅
- **سؤال «ما نشاطك؟»** عند أول تشغيل (`business_category.dart`): ثمانية
  أنشطة (مطعم، كافيه، أزياء، تجميل، حلويات، عقار، خدمات، متجر)، ولكل
  نشاط **مفرداته ومنافعه ودعوة إجراء وهاشتاقات خاصة به**. النص المولَّد
  صار من تقاطع النشاط × النبرة × المنصة بدل قالب عام: الكافيه يكتب عن
  التحميص والرائحة و«زورونا اليوم»، والعقار عن التشطيب والتمويل
  و«احجز معاينتك». دعوة الإجراء تظهر على زر التصميم نفسه، والنشاط
  قابل للتغيير من الإعدادات ويُحفظ دائمًا. ✅
- **حاسبة تكلفة الطباعة أثناء التصميم** (`print_cost_calculator.dart`):
  بطاقة قابلة للطي في شاشة السحر ومسار التصميم الرقمي تعرض السعر التقديري
  فورًا (منتج + مقاس + كمية) مع تحديث حيّ عند تغيير الاختيار، وزر «تابع
  لطلب الطباعة» ينتقل مباشرة لمسار الطباعة بالاختيار نفسه — تحوّل «المصمّم»
  إلى «مشترٍ» قبل أن يغادر شاشة التصميم بدل اكتشاف السعر بعد عدة خطوات. ✅
- **سلة مهملات للإعلانات** (`trashed_ad.dart` + `trash_screen.dart`): حذف
  إعلان من «إعلاناتي» لا يفقده — ينتقل لسلة المهملات مع شريط تنبيه فيه
  زر «تراجع» فوري، وشاشة سلة كاملة لاستعادة أو حذف نهائي لكل عنصر أو
  إفراغ الكل (بتأكيد لأنه نهائي)، وحذف تلقائي لما تجاوز 30 يومًا عند كل
  تحميل للتطبيق. أيقونة السلة في شريط «إعلاناتي» تحمل شارة بعدد العناصر. ✅
- **خط العلامة (Brand Kit)** (`brand_font.dart`): إضافة Cairo وAlmarai
  مضمَّنَين مع التطبيق إلى جانب Tajawal — يختار التاجر خطه من الإعدادات
  ويُطبَّق فورًا على عنوان ودعوة إجراء كل تصميم مولَّد (لا على واجهة
  التطبيق نفسها)، عبر `DefaultTextStyle.merge` في `AdDesignPreview` فلا
  حاجة لتعديل كل نص في القوالب الستة يدويًا. ✅
- **ترتيب معرض القوالب حسب النشاط والرواج** (`ad_template.dart`): كل قالب
  يحمل الآن `trendingRank` (ترتيب رواج ثابت) و`suitableFor` (الأنشطة التي
  يناسبها)، ودالة `orderTemplatesForBusiness` تقدّم القوالب المناسبة لنشاط
  التاجر المحفوظ أولًا ثم الأكثر رواجًا كترتيب ثانوي — شارة «🔥 رائج» تظهر
  على أعلى قالبين رواجًا. كل الحساب محلي بلا أي بيانات استخدام حقيقية بعد. ✅
- **قوالب المواسم المحلية** (`seasonal_theme.dart`): موسم اختياري (اليوم
  الوطني، رمضان، العيد، الجمعة البيضاء، موسم الرياض) يُختار من صفحة
  التفاصيل، فيضيف هاشتاقًا وجملة حملة احتفالية للنص المولَّد فوق مفردات
  النشاط لا بدلًا منها، ولون الموسم يتقدّم على لون المنتج المستخرَج تلقائيًا
  في لوحة التصميم (لكن يبقى لون العلامة Brand Kit الأسبق دائمًا)، وشارة
  ملوّنة بأعلى التصميم تُظهر اسم الموسم. كل هذا محلي بلا خادم — مواسم
  ذروة طلب حقيقية لا تغطيها قوالب Canva العربية (مجرد ترجمة لتصاميم
  غربية). ✅
- **تنويعات نصية أكثر لكل نشاط** (`business_category.dart`): كل نشاط صار
  يحمل 6 عبارات جذب و6 جمل منفعة (بدل 3) ودعوتي إجراء بديلتين إضافيتين —
  «إعادة التوليد» تُنتج صياغات مختلفة فعلًا بدل تكرار نفس الثلاث نسخ.
  الأصل يبقى أول خيار دائمًا (لا يتغيّر سلوك أي نص قائم)، والتنويع كله
  بذور محلية حتمية بلا أي خدمة توليد نصوص خارجية.
- **اقتراح الموسم القادم تلقائيًا** (`isSeasonApproaching`): شارة «قريبًا»
  تظهر على رقاقة الموسم في صفحة التفاصيل حين يكون التاريخ الميلادي الحالي
  قريبًا منه — اليوم الوطني (خلال 30 يومًا قبل 23 سبتمبر)، الجمعة البيضاء
  (منتصف نوفمبر)، موسم الرياض (أكتوبر–مارس). تعمّدنا **عدم** اقتراح رمضان
  والعيد تلقائيًا لأنهما بالتقويم الهجري المتغيّر ~11 يومًا كل سنة، ولا
  تقويم هجري مضمَّن في التطبيق — الاختيار اليدوي لهما يبقى الخيار الوحيد
  الموثوق بدل الادّعاء بدقة غير مضمونة.
- **إصلاح إتاحة بطاقة الإعلان المحفوظ** (`my_ads_screen.dart`): زرّا
  «نسخ النص» و«حذف» كانا متداخلين داخل شجرة الإعلان القابلة للفتح
  بالكامل، فيسقطهما محرك الوصولية (Accessibility Tree) كليًا — يظهران في
  DOM لكن قارئ الشاشة لا يصلهما ولا يقرأ اسمهما. اكتُشف الخلل أثناء تحقّق
  فعلي شامل بمتصفح حقيقي (لا اختبار وحدة) عبر فحص شجرة الوصولية الفعلية
  التي يستخدمها قارئ الشاشة، لا شجرة DOM أو شجرة Semantics الداخلية في
  Flutter وحدهما (كلتاهما بدتا سليمتين رغم الخلل الفعلي). الإصلاح: فصل
  بنيوي (الزرّان خارج شجرة النقر لفتح التصميم) + `Semantics.label` صريح
  + `Semantics.onTap` صريح (بلا حدث فعلي تُسقط شجرة الوصولية العقدة رغم
  ظهورها). ✅
- **خلفية مصمَّمة بديلة للتدرّج** (`assets/backgrounds/*.svg` +
  `ad_design_preview.dart`): 8 رسوم متجهة SVG حقيقية مضمَّنة مع التطبيق
  (زخرفة زاوية مستوحاة من كل نشاط — شوكة وسكين للمطاعم، فنجان قهوة
  للكافيهات، شمّاعة للأزياء، بريق للتجميل، كب كيك للحلويات، بيت للعقار،
  مفتاح ربط للخدمات، حقيبة تسوّق للمتاجر) تُلوَّن وقت التشغيل بلوحة
  التصميم نفسها (`flutter_svg` + `ColorFilter`) بدل تدرّج لوني محسوب فقط
  — خيار بديل يفعّله التاجر بمفتاح تبديل في صفحة التفاصيل، نمط قوالب
  Canva الجاهزة، ولا يغيّر أولوية ألوان اللوحة (العلامة ← الموسم ←
  المنتج ← النبرة). ✅
- **محرك الفن على الجهاز** (`lib/theme/art_palette.dart` +
  `lib/widgets/art_backdrop.dart` + `lib/widgets/art_text.dart`): الردّ على
  أدقّ نقد تلقّاه التطبيق — «قوالب بلا فنّ». ثلاث طبقات، كلها حساب على
  الجهاز بلا حصة ولا انتظار:
  - **انسجام لوني**: كان التصميم يُبنى على لون واحد ودرجة أغمق منه، فتخرج
    لوحة أحادية باهتة (كل شيء بنّي حول منتج بنّي). صار يُبنى على **علاقة**:
    مرافق على عجلة الألوان بأربعة أنواع انسجام (متقابل، منشقّ، ثلاثي،
    متجاور) يتناوبان حتميًّا بحسب نصّ الإعلان، وحياديّ مُلوَّن، وحبر يُختار
    بقياس تباين WCAG لا بالذوق — بسُلَّم ثلاثي (أبيض ← `#141728` ← أسود)
    يسدّ الشريط الذي كان يسقط فيه الخياران معًا.
  - **خلفيات مرسومة**: خمسة أنماط (بؤر متداخلة، إضاءة مسرح، أقواس، طبقات
    مائلة، ورق فاتح) بحبيبات حتمية تكسر «البلاستيكية» الرقمية — بدل تدرّج
    خطّي واحد كان يجعل القوالب العشرة تبدو قالبًا واحدًا. ومسرح للمنتج
    (هالة + ظلّ تماسّ) فلا يبدو قصاصةً ملصوقة.
  - **صفّ النص**: العنوان يُقاس ويُضبط حجمه بدل بتره بثلاث نقاط، ويُكسر
    كسرًا متوازنًا فلا يبقى سطر يتيم. والتكبير مشروط بمعرفة الارتفاع
    المتاح، وإلا دفع ما تحته خارج الإطار.
  - أثر جانبي مقصود: لم يبق في محرك القوالب لونٌ ثابت من هوية المنصّة —
    زرّ الحثّ والشارات كلها بلون **التاجر** الآن. ✅
- **السوق وواجهة العرض** (`lib/models/ad_service.dart` +
  `market_screen.dart` + `provider_screen.dart` + `storefront_screen.dart`):
  الاسم كان «سوق الدعاية والإعلان الشامل» والتطبيق أداةَ توليد وطباعة لا
  غير — من أراد مصوّرًا أو مصمّم هوية أو من يدير حملته خرج منه ولم يعد.
  الآن تبويب **السوق** في الموضع الثاني: سبعة أصناف خدمة (طباعة، تصميم،
  تصوير منتجات، مونتاج، إدارة حملات، لوحات، هدايا دعائية) ببحث وتصفية
  بالصنف والمدينة، وترتيب يوازن التقييم بعدد المراجعات. شبكة المطابع
  الموجودة تدخل الدليل تلقائيًا فلا تفترق قائمتان. ولكل مزوّد صفحة فيها
  أعماله وأسعاره وزمن ردّه، رأسها مرسوم **بمحرّك الفن نفسه** بلوحة مشتقّة
  حتميًّا من اسمه — فيتميّز بلا صورة غلاف، ولا يبدو السوق تبويبًا مستوردًا.
  والنصف الثاني: **متجري** — واجهة عرض التاجر تجمع هويته وشعاره وما حفظه
  من إعلانات، فيصير عارضًا لا مشتريًا فقط. ✅
- **صيغ اللوحة** (`lib/models/ad_format.dart`): كان التطبيق **يبيع طباعة
  لا يستطيع تصميمها** — الصيغة ثلاث سلاسل نصّية والمقياس `العرض ÷ ٤٠٠`
  أي مربّع مفترَض، بينما الكتالوج فيه رول أب ‎85×200‎ وكرت ‎9×5‎. صارت
  ثماني صيغ بنِسَب مأخوذة من الكتالوج نفسه، ولكلٍّ هامش آمن (أوسع في
  المطبوع لأن سكّين القصّ لا تقع على الخطّ) ومعامل حرف بحسب مسافة
  القراءة، وشرحٌ متى تُستعمل. وصنفان جديدان من التخطيط — العريض (كرت،
  بنر) والاستاند (رول أب) — تمرّ منهما القوالب الأحد عشر محتفظةً
  بشخصيّتها، فلا ينفجر العدد إلى ٨٨ تخطيطًا. النِّسَب التصميمية مقيسة من
  تشريح بنر كانفا حقيقي: هامش ٣٫٨٪، عنوان ٤٫٩٪ من العرض، ارتفاع سطر
  ١٫٤، منتج غير مركزي. ✅
- **معالجة الصورة داخل التصميم** (`lib/widgets/product_image.dart`): كان
  المحسّن يعمل عند الرفع ثم تُوضع الصورة في التصميم خامًا، فيرى المشاهد
  طبقتين لا لوحة. صارت تُعالَج كجزء منه — تباين وتشبّع، وتدرّج مزدوج
  خفيف، وتعتيم أطراف بلون اللوحة — من نقطة واحدة تعبرها كل صورة في كل
  قالب. والشدّة مقيسة لا مذوّقة: الصبغ الثقيل يوحّد الصورة بالتصميم لكنه
  يكذب على المشتري، فتُرك للأطراف حيث لا منتجَ يُكذَب عليه. ✅
- **مواصفة التصميم وطبيبها** (`lib/models/design_spec.dart` +
  `lib/services/spec_doctor.dart`): القطعتان الأوليان من محرّك التصميم
  المولَّد (تفصيله في قسمه أدناه). المواصفة لغة وسيطة بإحداثيات كسريّة
  وألوان كأدوار، والطبيب يفحص كل مخرَج قبل عرضه — الهامش الآمن، والتباين
  ≥ ٤٫٥، وتغطية النصوص، ووجود عنوان ودعوة — فيُصلح ما يُصلَح ويُبلّغ عمّا
  لا يُصلَح. ✅
- **لوحة تسجيل المزوّدين** (`supabase/migrations/20260818000000_service_providers.sql`
  + `lib/services/provider_directory.dart` + `provider_signup_screen.dart`):
  الدليل كان قائمةً نكتبها نحن في ملفّ داخل التطبيق — كل مزوّد جديد يحتاج
  بناءً ونشرًا وتحديثًا على كل جهاز. صار المزوّد يسجّل نفسه ثم يُراجَع ثم
  يظهر، فينمو السوق بلا إصدار جديد. ثلاثة قرارات تحكم التصميم: التسجيل
  لا يعني الظهور (الحالة تبدأ `pending`، والقراءة العامّة للمعتمَدين
  وحدهم)؛ والمزوّد لا يعتمد نفسه ولا يوثّق نفسه — وهذا مفروض بمشغّل في
  القاعدة لا بواجهة، لأن سياسة «عدّل صفّك» لا تمنع تعديل عمود بعينه
  وأي أداة HTTP تستطيع إرسال `status:'approved'`؛ ولا تقييمات مخترَعة —
  المزوّد الجديد يُعرض «جديد — بلا تقييمات بعد» لا بنجوم ممنوحة. والسوق
  يقرأ من الخادم ويسقط إلى المحلّي عند تعذّره، فلا تظهر شاشة فارغة في
  تبويب رئيسي حين تنقطع شبكة التاجر. ✅
- **أوّل صنف حقيقي في السوق — الطباعة**: صفحة المطبعة تعرض كتالوج
  الطباعة الفعلي (المنتجات والمقاسات والأسعار التي يحسب بها التطبيق
  الفاتورة، لا قائمة عرضٍ موازية تتقادم)، وكل صفّ يفتح مسار الطلب نفسه
  بالمنتج والمقاس مُختارَين. والسعر في بطاقة السوق يُحسب من الكتالوج
  فلا يَعِد الدليلُ بسعرٍ لا يجده التاجر. والعكس كذلك: من صمّم رول أب
  يجد «رول أب» مختارًا عند الطباعة لا «بنر». ✅
- 44 اختبارًا تغطي الرحلة والحفظ وعزل الخلفية والحسابات والدفع والتوجيه
  وBrand Kit (لون وخط) وشاشة الترحيب ومحرك القوالب ومعاينة المطبوع والمكتبة
  ومعرض القوالب (تصفح وترتيب حسب النشاط والرواج) ونشاط التاجر (بتنويعاته)
  وحاسبة التكلفة وسلة المهملات والمواسم المحلية (باقتراحها التلقائي)
  وإتاحة بطاقة الإعلان والخلفية المصمَّمة، وتحقق فعلي في المتصفح.

> خطوات فتح حسابات المطورين وتكاليفها في [`PUBLISHING.md`](./PUBLISHING.md).

> تفصيل ما يدفعه العميل مقابل كل صورة/فيديو وهوامش الربح في
> [`PRICING.md`](./PRICING.md).

## اقتصاديات اشتراك 29$ (109 ر.س) — لماذا هو مربح؟

الفكرة الخاطئة الشائعة: أن الاستضافة تكلف لكل مشترك بقدر اشتراكه. الواقع:

| البند | التكلفة الفعلية | ملاحظات |
|---|---|---|
| الاستضافة والبنية الخلفية | **أقل من 1 ر.س لكل مشترك** | تكلفة Firebase/Supabase إجمالية للتطبيق كله؛ أول آلاف المستخدمين ضمن الشريحة شبه المجانية |
| القوالب وعزل الخلفية والنصوص القوالبية | **0 ر.س** | تعمل على جهاز العميل (الطبقة 1) — لا تلمس أي خادم |
| توليد نصوص عبر AI API | ~0.01–0.05 ر.س للإعلان | مئات التوليدات شهريًا < 5 ر.س |
| توليد صور عبر AI API | ~0.05–0.2 ر.س للصورة | مشترك يولّد 100 صورة شهريًا ≈ 5–20 ر.س |
| توليد فيديو (الأغلى) | ~1–4 ر.س للمقطع القصير | يُضبط بحد عادل (مثلًا 20–30 فيديو/شهر ≈ 20–90 ر.س) |

**الخلاصة:** المشترك النموذجي يكلّف 5–25 ر.س شهريًا مقابل 109 ر.س →
هامش إجمالي 75–95%. الحماية من المستهلك الكثيف: **حدود استخدام عادلة +
نظام رصيد** للتجاوز (نمط AdCreative.ai نفسه). والأهم: الاشتراك ليس مصدر
الربح الوحيد — **هامش الطباعة 15–20% ورسوم التوصيل** يربحان من كل طلب
حتى من مستخدمي الخطة المجانية.

## عوائق الانتقال للخوادم الحقيقية (وما المطلوب من صاحب المشروع)

1. **حسابات لا يُنشئها غيرك**: مشروع Firebase، حساب ميسر التجاري (المفتاح
   السري)، مفاتيح مزودي الذكاء الاصطناعي — تحتاج بريدك وبياناتك البنكية.
2. **المفاتيح السرية تعيش على الخادم فقط** (Cloud Functions): وضعها في
   التطبيق يعني تسليمها لكل من يفك الحزمة.
3. **تحقق الدفع عبر Webhook**: الخادم يستقبل تأكيد ميسر مباشرة ولا يصدّق
   ادعاء التطبيق أن الدفع تم.
4. **واجهات النشر المباشر (Meta/TikTok)**: حساب مطور + توثيق نشاط تجاري +
   مراجعة تستغرق أسابيع — أطول عائق زمنيًا، يُبدأ به مبكرًا.
5. **حماية البيانات**: الالتزام بنظام حماية البيانات الشخصية السعودي (PDPL)
   ونسخ احتياطي للبيانات.
6. **تشغيل ومراقبة**: سجلات، تنبيهات أعطال، حدود معدل ضد إساءة الاستخدام.

**تحديث القرار (2026-08-10): الخادم الفعلي أصبح Supabase، لا Firebase.**
تم تزويد مشروع Supabase حقيقي (خطة مجانية) وربطه بهذا المستودع:

- **الرابط:** `https://fbbrkwragezhztffbvxf.supabase.co` (منطقة فرانكفورت
  eu-central-1، PostgreSQL 17.6). المشروع الأول أُنشئ خطأً في سيول
  (ap-northeast-2 — بعيدة جدًا عن السعودية)؛ نُقل التطبيق لمشروع جديد
  بنفس المخطط في فرانكفورت (انظر القسم أدناه لتفاصيل النقل).
- **قاعدة البيانات الأولية مُنشأة فعليًا** (لا خطة نظرية) عبر 3 جداول
  في مخطط `public`، كلها بـ Row Level Security مفعّلة بحيث لا يقرأ أي
  تاجر بيانات تاجر آخر:
  - `merchants` — حساب التاجر (مرتبط بـ `auth.users`)، اسم النشاط، الفئة.
  - `brand_identities` — هوية العلامة لكل تاجر (الألوان، النبرة، الشعار)
    — تُغذّي الطبقة 5 (الذاكرة/RAG) من خطة "عقل AdCraft".
  - `generation_logs` — سجل كل إعلان مولَّد (البرومبت، الناتج، التقييم،
    عدد النقرات) مع عمود `embedding vector(1536)` عبر امتداد `pgvector`
    (مثبَّت في مخطط `extensions` تحديدًا) — هذا هو أساس "حلقة التعلم"
    (الطبقة 6) لاحقًا.
- **مفتاح publishable (غير سرّي، آمن للتضمين في تطبيق Flutter):**
  `sb_publishable_l6cZctfpZSOquWbRdxg6hw_ulgI1kbM`. لم يُجلَب أو يُخزَّن
  أي مفتاح `service_role` (السرّي) في هذا المستودع — يبقى هذا خارج الكود
  دومًا، طبقًا لمبدأ "المفاتيح السرية تعيش على الخادم فقط" أدناه.
- **لم يُربط بعد بكود Flutter فعليًا** (لا حزمة `supabase_flutter`، ولا
  شاشات تسجيل دخول حقيقية) — التزويد الحالي هو البنية الخلفية فقط. لا
  يوجد بعد مزوّد ذكاء اصطناعي (Claude/OpenAI) ولا Edge Function توليد —
  هذا ما تحتاجه "م0" من خطة "عقل AdCraft" قبل أول توليد إعلان حقيقي.

### ربط `supabase_flutter` بالتطبيق (2026-08-10)

`AppState` (`lib/state/app_state.dart`) يقبل الآن `SupabaseClient?` اختياريًا:

- **عند تمرير عميل حقيقي** (كما يفعل `lib/main.dart` بعد
  `Supabase.initialize(...)`): `register`/`login` يستخدمان Supabase Auth
  الحقيقي (`signUp`/`signInWithPassword`)، ويُنشآن/يقرآن صفًا في
  `merchants` (`owner_name`, `business_name`, `category`)، وتُستعاد
  الجلسة تلقائيًا بعد إعادة فتح التطبيق عبر `_client.auth.currentUser`.
- **بلا عميل** (كل اختبارات الودجت الـ44 — `AppState()`/`AppState.load()`
  بلا وسيط): يبقى المسار المحلي القديم (تجزئة كلمة مرور مملّحة، تخزين في
  SharedPreferences) كما هو تمامًا — بلا أي اتصال شبكة، فبقيت الاختبارات
  حتمية وسريعة.
- `register`/`login` أصبحا `Future<String?>` (كانا متزامنين)؛ `auth_screen.dart`
  يعرض مؤشر تحميل أثناء الانتظار ويترجم أخطاء Supabase الشائعة للعربية.
- رابط المشروع ومفتاح publishable في `lib/services/supabase_config.dart`
  (غير سرّيين، آمنان للتضمين).

**قيد التحقق:** `flutter analyze` نظيف و44/44 اختبار ودجت ناجحة. التحقق
الكامل عبر متصفح حقيقي مع اتصال شبكي فعلي بـ Supabase **لم يكتمل** — بيئة
هذه الجلسة تمنع الوصول المباشر لـ `*.supabase.co` عبر سياسة صادرة تنظيمية
(كل الأدوات باستثناء أدوات MCP الخاصة بـ Supabase؛ محاولة تسجيل حساب عبر
متصفح حقيقي داخل هذه الجلسة أعادت `403` من بوابة السياسة، لا من Supabase
نفسه). التدفق البصري تم التحقق منه يدويًا (تعبئة النموذج، الانتقال بين
الشاشات، رسالة الخطأ الصحيحة تظهر عند فشل الشبكة) — لكن نجاح إنشاء حساب
فعلي عبر شبكة حقيقية يحتاج تجربة من متصفح/جهاز خارج هذه البيئة المقيّدة.

### نقل المشروع من سيول إلى فرانكفورت (2026-08-10)

المشروع الأول (`fygettwuhlmkjotqzwhs`) أُنشئ سهوًا في منطقة سيول
(ap-northeast-2)، وهي بعيدة جغرافيًا عن السعودية وتزيد زمن الاستجابة
لكل طلب. بما أنه كان فارغًا تمامًا (0 صفوف في الجداول الثلاثة، لا مستخدمين
مسجَّلين)، تم:

1. إنشاء مشروع جديد `zol-adcraft` في فرانكفورت (eu-central-1) — ضمن
   الخطة المجانية، بلا أي تكلفة إضافية (تحقّقتُ من التكلفة أولًا: 0$/شهر).
2. تطبيق نفس هجرة المخطط عليه (`merchants`, `brand_identities`,
   `generation_logs`, RLS، وامتداد `pgvector` في مخطط `extensions`) —
   بلا أي تحذيرات أمنية هذه المرة.
3. تحديث `lib/services/supabase_config.dart` ليشير للمشروع الجديد.

**ملاحظة مهمة:** أدوات Supabase المتاحة لي في هذه الجلسة **لا تتضمن حذف
مشروع** (`delete_project` غير موجودة ضمن الأدوات المتاحة) — لذلك لم
أستطع حذف مشروع سيول فعليًا كما طُلب. أوقفتُه (`pause_project`) بدلًا
من ذلك، وهو إجراء قابل للتراجع ولا يستهلك موارد وهو متوقف. لحذفه نهائيًا
يلزم صاحب الحساب فتح لوحة تحكم Supabase (supabase.com/dashboard) يدويًا:
Project Settings ← General ← Delete Project.

**لم يُنفَّذ عمدًا:** الامتدادات التي ذُكرت كمفيدة لمشروع ZadGo (PGroonga
للبحث العربي، PostGIS لإسناد أقرب سائق، pg_cron للمُهل الآلية) متاحة في
كتالوج الامتدادات الافتراضي لهذا المشروع الجديد أيضًا (نفس الكتالوج الذي
رأيناه في مشروع سيول) — لكن لم تُفعَّل ولم يُكتب أي كود يستخدمها، لأن لا
شيء في تطبيق **zol** الحالي يحتاجها بعد، وZadGo مستودع منفصل غير مرتبط
بهذه الجلسة. تفعيلها الآن بلا استخدام فعلي يكون بنية تحتية بلا داعٍ
ملموس — الأفضل تفعيلها عند وجود مهمة محددة تحتاجها، في الجلسة/المستودع
الذي يخصها.

### بناء APK أندرويد عبر GitHub Actions (2026-08-10) ✅ نجح

بيئة هذه الجلسة بلا Android SDK فلا يمكن بناء APK محليًا (تحقّق `flutter
doctor` من ذلك). أُضيف `.github/workflows/build-apk.yml` يفوّض البناء
لخوادم GitHub (JDK 21 + Flutter 3.44.9)، بهدف الحصول على APK حقيقي
يُثبَّت على جهاز فعلي لتجربة تسجيل حساب Supabase حقيقي (شبكة هذه الجلسة
تمنع الوصول المباشر لـ `*.supabase.co`، انظر القسم أعلاه).

**العائق:** `pay_android` (إضافة طرف ثالث) تحدد إعداد JVM قديمًا/متضاربًا
بين مهام Java وKotlin الخاصة بها داخليًا، وAGP 9 يرفض هذا التعارض بصرامة.
ست محاولات إصلاح في `android/build.gradle.kts` حتى الوصول للحل الناجح
(توثيق تعليمي داخل الملف نفسه لمن يواجه نفس المشكلة لاحقًا):

1. توحيد مباشر عبر `subprojects { tasks.withType... }` — فشل: AGP يكتب
   إعداده الخاص بعد هذا التوحيد فيفوز.
2. نفس التوحيد داخل `afterEvaluate` — فشل بنفس الطريقة: التسجيل من
   `subprojects{}` الجذر يحدث قبل أن تبدأ الوحدة الفرعية تقييم سكربتها
   أصلًا، فيصبح توحيدنا أول عنصر في طابور afterEvaluate، ويكسبه تسجيل
   AGP الداخلي المتأخر (من سكربت الإضافة نفسه).
3. نفس الفكرة لكن مع استثناء `:app` (كانت `evaluationDependsOn(":app")`
   تُقيّمها مبكرًا فيفشل استدعاء afterEvaluate عليها لاحقًا) — أصلح خطأ
   البناء الفوري لكن لم يحل التعارض الأصلي (نفس فشل المحاولة 2).
4. `gradle.projectsEvaluated{}` مع إعادة كتابة خواص مهام JavaCompile/
   KotlinCompile مباشرة — وحّد هدف JVM فعلًا (Java=17 صحيح) لكن **كسر
   classpath الترجمة**: فقدت `geolocator_android` الوصول لحزم `android.*`
   (100 خطأ) لأن إعادة كتابة الخاصية بعد اكتمال تهيئة AGP لا تُعيد ربط
   classpath المرتبط بها.
5. الإصلاح عبر DSL نفسه (`compileOptions`/`kotlin{}`) لحظة `apply()`
   الإضافة عبر `plugins.withId` — أصلح جانب Java بلا أي كسر classpath،
   لكن `pay_android` يحدد `kotlinOptions.jvmTarget="1.8"` صراحةً في سطر
   متأخر داخل سكربتها الخاص فيكسب على جانب Kotlin فقط.
6. **الحل النهائي:** إبقاء إصلاح Java عبر DSL (المحاولة 5) + إضافة
   `gradle.projectsEvaluated{}` **لمهام Kotlin فقط** (لا Java، تفاديًا
   لتكرار كسر classpath المحاولة 4) لتطبيق الهدف بعد اكتمال كل تقييم.

**النتيجة:** [Run #8 نجح](https://github.com/mohammed981-tjn/zol/actions/runs/31383143561)
— APK حقيقي (29.3 م.ب) منشور كـ artifact باسم `zol-apk`، صالح للتنزيل
والتثبيت على جهاز فعلي (تسجيله بمفتاح debug — طبيعي لمرحلة الاختبار،
يطلب أندرويد السماح بالتثبيت من مصدر غير معروف).

## استراتيجية الذكاء الاصطناعي — ثلاث طبقات (القرار المعماري الأهم)

**المبدأ: التجربة الأساسية تعمل بالكامل داخل الجهاز بدون أي خدمة خارجية.
السحابة ميزة اختيارية مدفوعة فقط، تُغطّى تكلفتها من الاشتراك.**

### الطبقة 1 — داخل الجهاز 100% (مجانية، بلا إنترنت) — الأولوية القصوى
1. **عزل خلفية صورة المنتج على الجهاز** عبر نموذج segmentation صغير
   (TFLite / ML Kit) — صورة المنتج تصبح قابلة للتركيب على أي خلفية.
2. **محرك قوالب تصميم**: قوالب إعلانية احترافية جاهزة (خلفيات، تراكيب،
   ألوان، خطوط) تُركَّب عليها صورة المنتج المعزولة + النص المولَّد،
   وتُصدَّر كصورة حقيقية قابلة للتحميل بمقاسات المنصات (مربع/ستوري/ريلز).
3. **توسيع مولّد النصوص القوالبي** الحالي (`lib/services/ad_generator.dart`):
   مزيد من القوالب والتنويعات لكل نبرة ومنصة وفئة منتج.

> هذه الطبقة وحدها تنتج «إعلانًا يبدو مصممًا بذكاء اصطناعي» — وهي نفس
> الطريقة التي تعمل بها معظم أدوات التصميم العالمية خلف الكواليس.

### الطبقة 2 — نموذج لغوي صغير على الجهاز (اختيارية، يمكن تأجيلها)
- Gemma عبر MediaPipe LLM Inference، أو Gemini Nano (أندرويد الحديث)،
  أو Apple Intelligence (آيفون) — لتنويع الصياغة العربية بدل القوالب الثابتة.
- التنبيه: حجم النموذج 0.5–2GB وجودة العربية «مقبولة» — لذلك ليست أولوية.

### الطبقة 3 — السحابة (ميزة الاشتراك المدفوع 29$ فقط، ليست أساس التطبيق)
- توليد صور إعلانية كاملة من الصفر وفيديو — يستحيل على الهواتف اليوم
  لأنه يحتاج معالجات رسومية ضخمة تُستأجر لثوانٍ لكل توليد.
- لماذا سحابة؟ الخياران الوحيدان لهذه القوة: استئجارها عند الطلب (سنتات
  لكل توليد، بلا صيانة)، أو شراء خوادم وإدارتها (أغلى وأعقد). الاستئجار
  عند الطلب هو الصواب لمشروع ناشئ.
- **يمكن إطلاق التطبيق وتشغيله تجاريًا بدون هذه الطبقة نهائيًا.**

## محرّك التصميم المولَّد — أربع قطع مجدوَلة

### السرّ الذي كشفناه في كانفا

قرأتُ بنرًا حقيقيًّا من واجهة كانفا المهيكلة فلم أجد صورة، بل **عناصر
بإحداثيات وأدوار**: نصّ بحجمه ولونه، ومستطيل تملؤه صورة. ولهذا يبقى نصّ
تصاميمهم قابلًا للتحرير — لم يُطبع في بكسلات قطّ. الآلية ثلاث طبقات:
النموذج اللغوي يُخرج **مواصفة تصميم**، والمولّد المرئي يُنتج **الخلفية
فقط**، والعارض يرسم المواصفة رسمًا حتميًّا.

**وأين نحن:** نموذجنا يُخرج **نصًّا فقط** (عنوان ووصف وهاشتاقات)،
والتخطيط مكتوب في Dart في أحد عشر قالبًا. فالذكاء عندنا **يملأ فراغات**
ولا **يُركّب تكوينًا** — وهذا سبب بقاء التطبيق «قوالب» مهما حُسّنت
القوالب: العلّة ليست في جودتها بل في أن الذكاء لا يصل إليها.

| القطعة | ما فيها | الحالة | التبعيّة |
| --- | --- | --- | --- |
| **١ — المواصفة** (`lib/models/design_spec.dart`) | لغة وسيطة: لوحة + عناصر بأدوار وإحداثيات **كسريّة** (٠–١ لا بكسلات، فتصلح لكل صيغة) وألوان **كأدوار** لا قيم (`complement` لا `#E1A83B`، فلا يعرف النموذج هوية التاجر ولا يضيّعها) | ✅ مُنجزة | — |
| **٢ — الطبيب** (`lib/services/spec_doctor.dart`) | يفحص كل مواصفة قبل عرضها: الهامش الآمن، والتباين ≥ ٤٫٥، وتغطية النصوص، ووجود عنوان ودعوة. **يُصلح ما يُصلَح ويُبلّغ عمّا لا يُصلَح** | ✅ مُنجز | ١ |
| **٣ — العارض** (`SpecRenderer`) | يرسم **أي** مواصفة بمحرّك الفن نفسه بدل أحد عشر تخطيطًا محفورًا. عندها يصير عدد التكوينات مفتوحًا بلا سطر Dart لكل واحد | ⬜ التالية | ١ + ٢ |
| **٤ — ربط النموذج** | `ad-copy` يُخرج `DesignSpec` مقيَّدًا بمخطط JSON بدل نصّ، مع أمثلة قليلة (few-shot) من قوالبنا القائمة، وإعادة الطلب حين يردّ الطبيب بعلّة غير قابلة للإصلاح | ⬜ | ٣ |

**لماذا الطبيب قبل العارض:** النموذج **سيُخرج تخطيطات رديئة** — نصّ فوق
نصّ، وحرف على حافّة القصّ، ولون لا يُقرأ. وكانفا تقيّد مولّدها بمكتبة
قوالب بشرية ضخمة؛ ونحن نقيّده **بالقياس**. وكل ما بُني في المشروع —
الحبر المقيس بـWCAG، والهامش الآمن للطباعة، وصفّ النصّ — يصير مدقّقًا
لا زينة. وهذا ما يجعل مخرَج النموذج صالحًا **للطباعة** لا جميلًا على
الشاشة فقط.

**وما لن نطابق كانفا فيه، صراحةً:** مكتبتها (ملايين العناصر والصور
المنسّقة). ولا يجب: ميزتنا التي لا تملكها هي العربية أوّلًا، والمواسم
المحلية، والطباعة والتوصيل في التطبيق نفسه. المطلوب أن يصل **ذكاؤنا**
إلى التصميم، لا أن نصير كانفا.

## تعدّد اللغات — مجدوَل على أربع دفعات

التطبيق اليوم عربيّ بالكامل: كل نصّ مكتوب حرفيًّا داخل الويدجت، والاتجاه
مفروض RTL، والخط Tajawal. إضافة الإنجليزية ليست ترجمة ملفّ — هي فصلُ
النصّ عن الشيفرة أوّلًا، وما لم يُفعل ذلك ستُترجَم كل شاشة يدويًّا مرّتين
ثم تتباعد النسختان.

**لماذا الآن وليس لاحقًا:** كل شاشة جديدة تُبنى بنصّ محفور تزيد كلفة
الفصل. الدفعة الأولى وحدها هي الاستثمار؛ ما بعدها إضافة ملفّ لكل لغة.

**الترتيب المقترح للّغات** بحسب سوق السعودية والخليج: الإنجليزية (المقيمون
والشركات)، ثم الأردية والهندية والبنغالية والتغالوغية (أكبر جاليات
العمالة وأصحاب المحال الصغيرة)، ثم الفرنسية والتركية عند التوسّع.

| الدفعة | ما فيها | المخرج | التبعيّة |
| --- | --- | --- | --- |
| **١ — البنية** | `flutter_localizations` + `intl` + `l10n.yaml`، استخراج كل نصّ إلى `lib/l10n/app_ar.arb`، توليد `AppLocalizations`، حذف `Directionality` المفروض والاعتماد على `Locale` | التطبيق يعمل كما هو بالضبط، لكن بلا نصّ محفور | — |
| **٢ — الإنجليزية** | `app_en.arb` كاملًا، مبدّل اللغة في الإعدادات (يُحفظ في `SharedPreferences`)، ضبط LTR/RTL تلقائيًّا، خط لاتيني بديل لـTajawal | تطبيق ثنائي اللغة | ١ |
| **٣ — نصوص الإعلانات** | المولّد القوالبي والمواسم وأصناف الخدمة تُترجَم؛ ونصّ الإعلان المولَّد يتبع لغةَ **الجمهور** لا لغةَ الواجهة (تاجر يقرأ إنجليزيًّا قد يعلن عربيًّا) — حقل «لغة الإعلان» في شاشة التفاصيل، ويُمرَّر إلى `ad-copy` على الخادم | إعلانات بلغتين | ٢ |
| **٤ — لغات الجاليات** | أردية، هندية، بنغالية، تغالوغية: ملفّ `.arb` لكل لغة + خطوط ديفاناغاري/بنغالية مضمَّنة | تغطية أكبر شرائح التجّار الصغار | ٢ |

**مصائد معروفة تُعالَج في الدفعة ١ لا بعدها:**
- الأرقام: العربية تستعمل الأرقام الهندية أحيانًا واللاتينية أحيانًا —
  يُوحَّد عبر `NumberFormat` بحسب `Locale` لا بالكتابة اليدوية.
- العملة والتاريخ: «ر.س» و«15%» محفورة اليوم في حاسبة التكلفة والطلبات.
- الجمع: «إعلان/إعلانات» يُعالَج بـ`plural` في ARB لا بشرط `if`.
- **القوالب**: محرّك التصميم يقيس النصّ ويكسره (`art_text.dart`) — القياس
  لا يفترض لغة، لكن `TextDirection` يجب أن يأتي من لغة **الإعلان** لا من
  لغة الواجهة، وإلّا خرج إعلان إنجليزي بمحاذاة عربية.
- الاختبارات: حارس يمنع عودة النصّ المحفور (لا سلاسل عربية حرفية داخل
  `lib/screens`) — بدونه يتسرّب النصّ المحفور مجدّدًا خلال أسابيع.

## الأولوية القادمة — مستوحاة من مراجعة Canva

ثلاث خطوات كلها **على الجهاز بلا خادم ولا تكلفة تشغيل**. القاعدة الحاكمة:
لا نحاول أن نصير Canva (أداة تصميم عامة)، بل نأخذ منها ما يخدم ميزتنا
التي لا تملكها: **إعلان → طباعة → توصيل محليًا**.

1. ~~تبويب «القوالب» بمعرض متصفَّح~~ ✅ أُنجز، وكذلك ترتيب «الرائج» ✅
   والمواسم المحلية ✅ (أدناه).
2. ~~سؤال «ما نشاطك؟» ونصوص مخصصة لفئته~~ ✅ أُنجز، وكذلك ربط الفئة
   بترتيب القوالب في المعرض ✅ وتنويعات نصية أكثر لكل نشاط ✅ (أعلاه).
3. ~~معاينة المطبوع + تفعيل المكتبة~~ ✅ أُنجزت (أعلاه).

**✅ القائمة الثلاثية أعلاه مُنجَزة بكامل تحسيناتها الفرعية.** ما تبقّى من
هذه المرحلة على الجهاز فقط قوالب رسومية مخصصة لكل موسم (تحسين اختياري) —
كل ما بعدها يحتاج خادمًا (أدناه).

### أفكار إضافية تميّزنا عن Canva (لاحقًا)

- ~~قوالب المواسم المحلية~~ ✅ أُنجزت، وكذلك ~~اقتراح الموسم تلقائيًا~~ ✅
  — تحسين لاحق متبقٍّ: قوالب رسومية مخصصة (لا شارة فقط) لكل موسم.
- ~~حاسبة تكلفة الطباعة أثناء التصميم~~ ✅ أُنجزت.
- ~~**سوق المطابع**: تقييمات وأسعار واختيار~~ ✅ أُنجز وتجاوز حدّه: صار
  **سوق خدمات دعاية كاملًا** لا سوق مطابع (أدناه).
- ~~سلة مهملات / تراجع عن الحذف~~ ✅ أُنجزت.

## بقية الخطوات بالأولوية

1. ~~رفع صورة حقيقي~~ ✅ أُنجز.
2. ~~الطبقة 1: محرك القوالب + التصدير PNG + عزل الخلفية~~ ✅ أُنجز أساسها —
   تحسين لاحق: نموذج تجزئة ML للخلفيات المعقدة + مزيد من القوالب.
3. ~~حفظ دائم للبيانات (محلي)~~ ✅ أُنجز عبر SharedPreferences —
   يبقى النقل لتخزين سحابي مع الحسابات.
4. ~~حساب تاجر (محلي)~~ ✅ أُنجز — يبقى استبدال المخزن المحلي بـ
   Firebase Auth للمزامنة بين الأجهزة عند بناء الخادم.
5. ~~بوابة دفع (ميسر)~~ ✅ أُنجزت — للتفعيل الحقيقي مرِّر المفتاح عند البناء:
   `flutter build apk --dart-define=MOYASAR_PUBLISHABLE_KEY=pk_live_xxx`.
6. **شبكة المطابع**: ~~التوجيه التلقائي للأقرب~~ ✅ أُنجز محليًا — يبقى
   لوحة تحكم المطابع الشريكة (تحتاج خادمًا؛ المرحلة الأولى تكفيها 3 مطابع
   بتعاقد يدوي وتبليغ عبر واتساب/بريد).
7. **نشر مباشر** عبر واجهات المنصات (Meta/TikTok) بدل التحميل اليدوي —
   ابدأ إجراءات حساب المطور مبكرًا (مراجعتها تستغرق أسابيع).
8. ~~Brand Kit (لون وشعار وخط العلامة)~~ ✅ أُنجز بكامله.
9. **تعدّد اللغات — الدفعة ١ (البنية) ثم ٢ (الإنجليزية)**: تفصيلها في
   قسم «تعدّد اللغات» أعلاه. موضعها هنا مقصود — قبل التحليلات والطبقة
   السحابية، لأن كلفتها تتضخّم مع كل شاشة تُبنى بنصّ محفور، بينما
   التحليلات كلفتها ثابتة متى بُنيت.
10. **لوحة تسجيل المزوّدين** (تكملة السوق): مبنيّة ✅ — الجدول والسياسات
    والحارس وشاشة التسجيل وقراءة السوق من الخادم (أعلاه). **يبقى تطبيق
    الهجرة على مشروع `zol-adcraft`**: `20260818000000_service_providers.sql`
    جاهز في المستودع ولم يُطبَّق بعد (موصّل Supabase كان مفصولًا وقت
    البناء). حتى يُطبَّق، يقرأ السوق المحلّي وحده — ولا شيء ينكسر.
    والمتبقّي بعده: توجيه **طلب عرض السعر** إلى المزوّد فعلًا (بريد أو
    واتساب)، والتقييمات بعد أول طلب حقيقي يُقيَّم.
11. **محرّك التصميم المولَّد — القطعتان ٣ و٤** (العارض ثم ربط النموذج):
    تفصيلها في قسم «محرّك التصميم المولَّد» أعلاه. موضعها هنا مقصود —
    هي الجواب على «لماذا يبقى التطبيق قوالب»، وكل تحسين للقوالب قبلها
    يرفع سقفًا يبقى أحد عشر.
12. **تعدّد اللغات — الدفعتان ٣ و٤** (لغة الإعلان المولَّد، ثم لغات
    الجاليات).
13. **تحليلات أداء الإعلانات** بعد النشر لتغذية «درجة التوافق» ببيانات
    حقيقية — نمط AdCreative.ai.
14. **الطبقة 3 السحابية** — عند توفر قاعدة مشتركين تغطي تكلفتها.
